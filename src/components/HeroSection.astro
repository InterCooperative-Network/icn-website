---
// src/components/HeroSection.astro
export interface Props {
  // Pretitle like "OPEN - FEDERATED - COOPERATIVE"
  pretitle?: string;
  
  // Main multi-line headline
  headline1?: string;
  // headline2 and headline3 will be effectively removed by not using them or defaulting to empty
  // tagline is already removed
  
  // Descriptive paragraph
  description?: string;
  
  primaryButtonText?: string;
  primaryButtonLink?: string;
  secondaryButtonText?: string;
  secondaryButtonLink?: string;
  
  showImage?: boolean;
  imageUrl?: string;
  imageAlt?: string;
}

const {
  pretitle = "OPEN • FEDERATED • COOPERATIVE",
  headline1 = "Infrastructure for People—Not Empires.",
  description = "",
  primaryButtonText = "Get Started",
  primaryButtonLink = "/get-started",
  secondaryButtonText = "How It Works",
  secondaryButtonLink = "#/how-it-works",
  showImage = true,
  imageUrl = "/images/hero-globe.png", 
  imageAlt = "Abstract network globe representing interconnectedness"
} = Astro.props;
---

<section class="bg-midnight-navy text-white py-16 md:py-20 lg:py-24 relative overflow-hidden">
  <div class="container mx-auto px-4 relative z-10">
    <div class="flex flex-col md:flex-row items-center gap-10 md:gap-12 lg:gap-16">
      <!-- Text Content Area (Left) -->
      <div class="md:w-[55%] text-center md:text-left">
        {pretitle && (
          <p class="text-sky-blue font-semibold tracking-widest text-xs sm:text-sm uppercase mb-5">
            {pretitle}
          </p>
        )}
        <h1 class="text-3xl sm:text-4xl lg:text-5xl font-bold mb-6 text-white">
          {headline1}
        </h1>
        
        <div class="flex flex-col sm:flex-row justify-center md:justify-start gap-4 sm:gap-5 mt-8">
          <a href={primaryButtonLink} class="button button-primary text-base sm:text-lg px-7 py-3">
            {primaryButtonText}
          </a>
          <a href={secondaryButtonLink} class="button button-secondary text-base sm:text-lg px-7 py-3">
            {secondaryButtonText}
          </a>
        </div>
      </div>
      
      <!-- Image Area (Right) -->
      {showImage && (
        <div class="md:w-[45%] mt-10 md:mt-0 flex items-center justify-center md:justify-end">
          <img src={imageUrl} alt={imageAlt} class="w-full max-w-xs sm:max-w-sm md:max-w-md lg:max-w-lg xl:max-w-xl object-contain" />
        </div>
      )}
    </div>
  </div>
</section>

<style>
  h1 {
    font-family: var(--font-sans);
    line-height: 1.1; /* Slightly adjusted for the new multi-line content */
    letter-spacing: -0.01em; /* A touch tighter for large display text */
  }
  /* Ensure enough vertical space if image is shorter than text on md screens */
  @media (min-width: 768px) {
    .flex-col.md\:flex-row > div:last-child {
      align-self: stretch; /* Make image container take full height of flex row if needed */
    }
    .flex-col.md\:flex-row > div:last-child img {
      height: 100%; /* Allow image to scale within its container if it's tall */
      max-height: 65vh; /* But don't let it get too excessively tall */
    }
  }
</style> 